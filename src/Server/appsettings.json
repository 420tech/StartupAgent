{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "JwtSigningKey": "{{ Base64-encoded 32-byte key - REPLACE with value from Key Vault }}",
    "MagicLinkSecret": "{{ Base64-encoded 32-byte key - REPLACE with value from Key Vault }}"
  }
}
