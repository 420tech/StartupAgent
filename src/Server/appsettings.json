{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "{{ Replace with Azure SQL connection string or local SQL Server connection string }}"
  },
  "Auth": {
    "JwtSigningKey": "{{ Base64-encoded 32-byte key - REPLACE with value from Key Vault }}",
    "MagicLinkSecret": "{{ Base64-encoded 32-byte key - REPLACE with value from Key Vault }}"
  }
}
